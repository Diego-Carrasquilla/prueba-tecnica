{
    "scripts": {
        "install:all": "npm install && cd ../python-api && pip install -r requirements.txt",
        "setup": "echo 'Asegúrate de configurar el archivo .env' && npm install",
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
        "type-check": "tsc --noEmit"
    }
}